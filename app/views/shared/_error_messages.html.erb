<% if object.errors.any? %>
  The form contains
  <%= pluralize(object.errors.count, "error") %> <br>
  <ul>
  <% object.errors.full_messages.each do |error| %>
    <li><%= error %></li>
  <% end %>
  </ul>
<% end %>
